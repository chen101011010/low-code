// export default {
//     install(app: { component: (arg0: string, arg1: Promise<any>) => void }) {
//         ['NButton'].forEach(name => {
//             app.component(name, import('./' + name));
//         });
//     },
// };
