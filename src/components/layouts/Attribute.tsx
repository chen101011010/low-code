import { defineComponent } from 'vue';

export default defineComponent({
    name: 'Attribute',
    setup() {
        return () => (
            <>
                <div>右侧属性</div>
            </>
        );
    },
});
