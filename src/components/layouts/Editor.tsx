import { defineComponent } from 'vue';

export default defineComponent({
    name: 'Editor',
    setup() {
        return () => (
            <>
                <div>中间编辑器</div>
            </>
        );
    },
});
