import { defineComponent } from 'vue';

export default defineComponent({
    name: 'List',
    setup() {
        return () => (
            <>
                <div>左侧列表</div>
            </>
        );
    },
});
